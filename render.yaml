services:
  - type: web
    name: vaultledger
    runtime: node
    rootDir: web-app
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
